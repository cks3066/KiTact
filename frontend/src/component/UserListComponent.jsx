import React from 'react';
import ApiService from '../ApiService';

const UserListComponents = (props) => {
    const [users,setUsers] = React.useState([]);
    const [message,setMessage] = React.useState();

    return null;
}

export default UserListComponents;